#include <stdio.h>

int main() {
   
   int n, i, q;

   scanf("%d", &n);

   for (i=1; i<=n; i++){
      if (i%2==0){
         q = i * i;
         printf("%d^2 = %d\n", i, q);
      }
   }
   
    return 0;
}
