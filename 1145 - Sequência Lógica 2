#include <stdio.h>

 int main() {

int n, i, fim, t, m;

   m = 0;

   scanf("%d %d", &n, &fim);

   for (i = 1; i <= fim/n ; i++){
     for (t = 1; t <= n-1; t++){
        m++;
       printf("%d ", m);
     }
     m++;
     printf("%d\n", m);
   }

   
   
return 0;

}
