#include <stdio.h>

 int main() {

int n, i;
   int arr[10];

   scanf("%d", &n);

   for (i = 0; i <= 9; i++){

     if (i == 0){
       arr[i] = n;
     }else{
       arr[i] = 2 * n;
       n = 2 * n;
     }

     printf("N[%d] = %d\n", i, arr[i]);
   }
   
return 0;

}
